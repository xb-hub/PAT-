#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n, m, k, q;
    cin >> n >> m >> k >> q;
    vector<int> times(k);
    for(int i; i < k; i++)
    {
        cin >> times[k];
    }
}