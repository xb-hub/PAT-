#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int p, g;
    cin >> p >> g;
    vector<int> array(p), team(p);
    for(int i = 0; i < p; i++)
    {
        cin >> array[i];
    }
    for(int i = 0; i < p; i++)
    {
        cin >> team[i];
    }
    int num = p / g;
    while (num > 1)
    {
        for(int i = 0; i < num; i++)
        {
            for(int j < 0)
        }
    }
    
}